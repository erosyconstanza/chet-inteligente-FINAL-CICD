name: chef_inteligente
description: Una aplicación de Flutter robusta e intuitiva para gestionar tu inventario de cocina y generar recetas con IA.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_riverpod: ^2.4.9
  google_generative_ai: ^0.2.0
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo.png
